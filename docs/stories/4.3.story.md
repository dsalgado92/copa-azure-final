# Story 4.3 — Consolidação (testes): integração da idempotência SQL + unit tests do `gatewayTrust.js` + guards fail-closed de startup

> **Epic:** [EPIC-004 — "Nível Produção"](../epics/EPIC-004-nivel-producao.md)
> **Status:** Ready
> **Created:** 2026-07-01 by @sm (River)
> **Sprint context:** Story de squad, **net-new de testes** — não muda comportamento de produção, só adiciona cobertura onde hoje há zero ou cobertura só-manual. Continua na branch `phase-08-hardening` (criada pela Story 4.1). **Paralela** ao núcleo Blindar (4.1/4.2) — sem dependência de conteúdo, só de branch compartilhada.
> **Depends on:** Story 4.1 (branch `phase-08-hardening` já existe) — **sem dependência de conteúdo** (roda em paralelo; os testes desta story continuam válidos independentemente da ordem de merge com 4.1/4.2)
> **Blocks:** nenhuma story subsequente diretamente — contribui ao **SC-5** (Consolidação verde) do épico
> **Source (Art. IV — rastreabilidade):** [ADE-009](../architecture/ade-009-network-secrets-service-identity.md) §Consequences ("Consolidação P0/P1 conexa... roteados a @sm/@dev/@qa — não viram invariante aqui") + §Impact on EPIC-004 · plano `giggly-mapping-hearth.md` §5 (itens 2, 4, 6 da auditoria @qa) · EPIC-004 tabela de Stories (linha S3) e tabela "Como a ADE-009 amarra o escopo" (§Consequences → 4.3)
>
> **Nota de numeração (@sm AUTO-DECISION):** Terceira story da série 4.x.
>
> **Nota de granularidade (@sm AUTO-DECISION):** O épico deixou a critério do `@sm` fundir 4.3+4.4 numa única "Consolidação". **Decisão: manter separadas** (default do épico) — 4.3 é **net-new sem mudança de comportamento** (risco baixo, só adiciona testes) enquanto 4.4 **muda comportamento** no pipeline do gateway (risco médio, exige `@architect` consult). Perfis de risco e gate distintos justificam duas stories.

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - "test-design"
  - "coverage-analysis"
  - "regression-validation"
test_design_support: "@qa"
```

**Rationale:** Story 100% de testes (integração + unit), sem mudança de comportamento de produção. `@dev` implementa; `@qa` (Quinn) desenha os cenários de teste (idempotência sob concorrência, timing-safety, fail-closed) e é o gate — não `@architect`, porque nenhuma invariante arquitetural nova está sendo decidida aqui (a ADE-009 já roteou explicitamente esta consolidação como "não invariante", §Consequences).

---

## Story

**Como** integrante do squad de desenvolvimento do Living Lab (papel `@dev`, com o desenho de teste de `@qa`, sob o gate `@qa`),
**eu quero** adicionar um teste de integração real para a idempotência do INSERT em `purchases` (hoje só validada manualmente/e2e), testes unitários para o `gatewayTrust.js` (hoje com **zero** testes no backend Node), e testes para os guards fail-closed de startup (config ausente → `InvalidOperationException`, hoje sem cobertura em nenhum dos 3 serviços .NET),
**para que** uma regressão futura nesses 3 mecanismos críticos (idempotência de compra, confiança do gateway, config fail-closed) seja pega por CI, não descoberta em produção — fechando a lacuna que a ADE-009 explicitamente aponta: *"guards de startup sem teste (regressão do `common` passaria hoje)"* e *"backend Node = 0 testes"*.

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita o trecho de código real verificado (alvo do teste) e a lacuna de cobertura confirmada por inspeção do repositório nesta story.

### Bloco A — Continuação de branch

1. **AC-1 — Story continua na branch `phase-08-hardening`** (criada pela Story 4.1) — nenhuma branch nova. Pode ser desenvolvida em paralelo às Stories 4.1/4.2 (net-new, sem tocar produção).
   *(Fonte: Story 4.1 AC-1; EPIC-004 dependências entre stories — "4.3 paralela".)*

### Bloco B — Integração: idempotência SQL real (ADE-009 §Consequences item 2 / plano §5 item 2)

2. **AC-2 — Novo teste de integração cobre `PurchaseRepository.InsertPurchaseAsync` contra um SQL Server REAL** (não mockado): usa **Testcontainers.MsSql** (pacote NuGet — confirmado ausente hoje em qualquer `.csproj` do repo, `grep Testcontainers` = 0 ocorrências) para subir um container efêmero do SQL Server, aplicar o schema real (`fifa2026-api/database/schema.sql` — reúso, não recriação de DDL) e as migrations necessárias (`ticket_categories`/`matches`/`teams` mínimos para o teste), e confirmar que **duas** chamadas com o **mesmo** `CorrelationId` retornam `InsertOutcome.Inserted` na primeira e `InsertOutcome.Duplicate` na segunda — via a violação REAL do índice único (`SqlException` 2627/2601, verificado `PurchaseRepository.cs` linhas 111-119), não uma exceção simulada em mock. Hoje essa idempotência só é validada **manualmente/e2e** (ADE-009 §Consequences: "teste de integração da idempotência SQL real, hoje só e2e manual").
   *(Fonte: verificado `PurchaseRepository.cs` linhas 17-20 (`SqlUniqueViolation = 2627`, `SqlDuplicateKey = 2601`), 111-119 (catch); grep `Testcontainers` em `src/` = 0 ocorrências; ADE-009 §Consequences item de Consolidação.)*

3. **AC-3 — O teste cobre também o caso de concorrência (2 requests simultâneas, mesmo `CorrelationId`):** ambas disparadas em paralelo (`Task.WhenAll`) contra o mesmo container — exatamente **uma** retorna `Inserted`, a outra `Duplicate` (nunca as duas `Inserted`, nunca exceção não tratada) — prova que a defesa é a violação do índice único no banco (ADE-000 Inv 4: "NUNCA SELECT-then-INSERT, evita TOCTOU race"), não uma checagem prévia frágil.
   *(Fonte: verificado comentário `PurchaseRepository.cs` linha 13 — "NUNCA SELECT-then-INSERT, evita TOCTOU race"; ADE-000 Inv 4.)*

4. **AC-4 — Graceful degradation se Docker indisponível:** o teste de integração é anotado/categorizado de forma que ambientes sem Docker (ex.: alguns runners locais) possam pulá-lo explicitamente (ex.: `[Trait("Category","Integration")]` + filtro no `dotnet test`, ou skip condicional via `DockerAvailableFact`-like helper) — não quebra o build geral; CI (GitHub Actions `ubuntu-latest`, que já tem Docker) roda o teste normalmente.
   *(Fonte: boa prática de testes de integração com Testcontainers — direção, não código; nenhum mecanismo de skip específico é inventado além do já padrão em xUnit.)*

### Bloco C — Unit tests do `gatewayTrust.js` (ADE-009 §Consequences item 4 / plano §5 item 4)

5. **AC-5 — Framework de teste JS adicionado ao backend v1:** `fifa2026-api/package.json` **não tem nenhuma dependência de teste hoje** (verificado — sem `jest`/`mocha`/`vitest` em `devDependencies`; `scripts` só tem `start`/`dev`). Esta AC adiciona **Jest** (escolha natural para o ecossistema Node/Express já em uso — `express`, `jsonwebtoken`, `bcryptjs`) como `devDependency` + um script `"test": "jest"` em `package.json`.
   *(Fonte: verificado `fifa2026-api/package.json` completo — 0 dependências de teste; glob `fifa2026-api/**/*.test.js` e `*.spec.js` e `__tests__/**` = 0 arquivos.)*

6. **AC-6 — Testes unitários de `gatewayTrust.js` cobrem os cenários que hoje têm zero cobertura automatizada:**
    - `safeEqual(a, b)`: retorna `true` só quando ambos são strings de mesmo tamanho e conteúdo idêntico; retorna `false` (nunca lança) para tipos não-string, tamanhos diferentes, ou valores divergentes (verificado `gatewayTrust.js` linhas 28-38).
    - `gatewayTrustMiddleware`: com `GATEWAY_SHARED_SECRET` configurado e `X-Gateway-Key` correto → `req.user` populado (`role: 'admin', source: 'gateway'`) e `next()` chamado (linhas 49-56).
    - Com `GATEWAY_SHARED_SECRET` **vazio/ausente** → cai no fluxo legado (`authMiddleware` → `adminMiddleware`), **independente** do header recebido (linha 60) — prova o comportamento fail-open-para-legado documentado nas linhas 17-18.
    - Com secret configurado mas header **ausente ou incorreto** → cai no fluxo legado (não autentica como admin via gateway) — nunca "meio-autenticado".
    *(Fonte: verificado `gatewayTrust.js` linhas 1-64 completo — arquivo lido integralmente nesta story.)*

7. **AC-7 — Teste NÃO precisa provar tempo-constante empiricamente (limite reconhecido):** medir timing em microbenchmark de unit test é frágil/não-determinístico; o teste confirma o **comportamento funcional** de `safeEqual` (retorna `false` seguramente para tamanhos diferentes, sem lançar) — a garantia de tempo-constante em si vem de usar `crypto.timingSafeEqual` (API nativa do Node, já correta no código, linha 37) e não é reprovada por unit test. Documentado como limitação conhecida, não fingida como coberta.
    *(Fonte: honestidade de escopo, Art. IV — não inventar uma garantia de teste que não é praticável.)*

### Bloco D — Guards fail-closed de startup (ADE-009 §Consequences item 6 / plano §5 item 6)

8. **AC-8 — Guards de configuração ausente no Gateway testados (hoje sem cobertura):** `Gateway/Program.cs` lança `InvalidOperationException` em 4 pontos verificados (linhas 245-252, 254-260, 278-285, 287-293) quando `Jwt:CiamTenantId`/`Jwt:CiamClientId`/`Jwt:AdminTenantId`/`Jwt:AdminClientId` estão ausentes ou o tenant é `"common"`. Novo(s) teste(s) em `Fifa2026.V2.Gateway.Tests` (reusando `GatewayTestFixture`/`WebApplicationFactory`, o mesmo padrão já usado por `JwtValidationTests.cs`/`DualIssuerTests.cs`) confirmam que **iniciar** o host com configuração faltando/`"common"` lança/falha a inicialização — regressão aqui hoje **passaria silenciosamente** (nenhum teste atual cobre esses 4 guards).
   *(Fonte: verificado `Gateway/Program.cs` linhas 245-293 — 4 blocos de guard; listagem de `Fifa2026.V2.Gateway.Tests/` confirma ausência de arquivo dedicado a esses guards.)*

9. **AC-9 — Guards de `SqlConnectionString` ausente testados em `PurchaseRepository`/`FifaQueryRepository` (Functions/McpServer):** ambos os construtores lançam `InvalidOperationException` quando `configuration["SqlConnectionString"]` é nulo/vazio (verificado `PurchaseRepository.cs` linhas 25-30, `FifaQueryRepository.cs` linhas 22-28). Novo teste unitário direto (instancia a classe com um `IConfiguration` sem a chave, sem precisar de `WebApplicationFactory`) confirma o `throw`. Hoje **nenhum teste existente** cobre esse caminho (confirmado: nenhum arquivo em `Fifa2026.V2.Functions.Tests`/`Fifa2026.V2.McpServer.Tests` testa o construtor com config ausente).
   *(Fonte: verificado `PurchaseRepository.cs` linhas 22-31; `FifaQueryRepository.cs` linhas 17-28.)*

### Bloco E — Regressão e anti-hallucination

10. **AC-10 — Regressão: nenhum teste existente quebra.** `dotnet test` nos 4 projetos .NET (`Functions.Tests`, `McpServer.Tests`, `Gateway.Tests`, `FlowEvents.Tests`) e `npm test` no backend Node — 0 falhas, 0 regressão fora do escopo desta story (novos testes apenas **adicionam** cobertura).
    *(Fonte: EPIC-004 SC-5 "novos testes... passando".)*

11. **AC-11 — Anti-hallucination (Art. IV):** `Testcontainers.MsSql` e `Jest` são pacotes REAIS amplamente usados nos respectivos ecossistemas (.NET/Node) — nenhum nome de pacote inventado. Os códigos de erro SQL (`2627`, `2601`) e as linhas de guard citadas foram verificados por leitura direta do código nesta story, não de memória.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Architecture (consolidação de qualidade — fecha lacunas de teste que a ADE-009 roteou explicitamente)
**Secondary Type(s):** Database (teste de integração real contra SQL), Security (testes do `gatewayTrust.js`, mecanismo de confiança serviço-a-serviço)
**Complexity:** MEDIUM (net-new, sem mudança de comportamento — mas a introdução de Testcontainers é uma nova peça de infraestrutura de teste, e testar guards de startup em `Program.cs` top-level exige o padrão `WebApplicationFactory` já estabelecido)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — implementação dos testes
- @qa (Quinn) — desenho dos cenários (concorrência, fail-closed, timing) + gate final

**Supporting Agents:**
- @architect (Aria) — nenhuma, exceto se `@qa` escalar dúvida sobre escopo de invariante

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco em nenhum teste "falso-positivo" (que sempre passa independente do comportamento real)
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — confirmar que os novos testes rodam em CI (Docker disponível no runner)
- [ ] Pre-Deployment: N/A — esta story não deploya

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix
- HIGH issues: auto_fix (documenta como debt se falhar)

### CodeRabbit Focus Areas

**Primary Focus:**
- Teste de idempotência realmente usa violação de índice REAL (não mock de `SqlException`) — senão não é "integração"
- `gatewayTrust.js` tests cobrem o caminho fail-open-para-legado (secret vazio), não só o caminho feliz

**Secondary Focus:**
- Testes de guard não deixam configuração real (tenant IDs reais) hardcoded — usam valores de teste sintéticos
- `Testcontainers.MsSql` isolado por teste (sem contaminação entre testes paralelos)

---

## Tasks / Subtasks

### Task 1 — Branch (AC: 1)

- [ ] 1.1 Confirmar continuidade em `phase-08-hardening` (criada pela Story 4.1).

### Task 2 — Integração: idempotência SQL (AC: 2, 3, 4)

- [ ] 2.1 Adicionar `Testcontainers.MsSql` a `Fifa2026.V2.Functions.Tests.csproj`.
- [ ] 2.2 Criar fixture que sobe o container, aplica `fifa2026-api/database/schema.sql` (+ seed mínimo de `matches`/`teams`/`ticket_categories` necessário para o INSERT resolver via JOIN).
- [ ] 2.3 Novo teste (`PurchaseRepositoryIntegrationTests.cs`, sugestão de nome): 2 chamadas sequenciais mesmo `CorrelationId` → `Inserted` depois `Duplicate`.
- [ ] 2.4 Novo teste: 2 chamadas paralelas (`Task.WhenAll`) mesmo `CorrelationId` → exatamente uma `Inserted`.
- [ ] 2.5 Categorizar os testes (`[Trait("Category","Integration")]` ou equivalente) para permitir skip gracioso sem Docker.

### Task 3 — `gatewayTrust.js` unit tests (AC: 5, 6, 7)

- [ ] 3.1 Adicionar `jest` a `fifa2026-api/package.json` `devDependencies` + script `"test": "jest"`.
- [ ] 3.2 Criar `fifa2026-api/src/middleware/__tests__/gatewayTrust.test.js` (ou local equivalente): testes de `safeEqual` (tipos/tamanhos/valores).
- [ ] 3.3 Testes de `gatewayTrustMiddleware`: secret+header corretos → `req.user`/`next()`; secret vazio → fluxo legado; secret configurado + header errado/ausente → fluxo legado.
- [ ] 3.4 Documentar (comentário no teste) a limitação reconhecida de não medir tempo-constante empiricamente.

### Task 4 — Guards fail-closed de startup (AC: 8, 9)

- [ ] 4.1 Novo(s) teste(s) em `Fifa2026.V2.Gateway.Tests` cobrindo os 4 guards de config ausente/`"common"` (`Jwt:CiamTenantId`/`CiamClientId`/`AdminTenantId`/`AdminClientId`) via `WebApplicationFactory` com configuração de teste faltando cada chave.
- [ ] 4.2 Novo teste unitário direto em `Fifa2026.V2.Functions.Tests`: `PurchaseRepository` construído com `IConfiguration` sem `SqlConnectionString` → `Assert.Throws<InvalidOperationException>`.
- [ ] 4.3 Novo teste equivalente em `Fifa2026.V2.McpServer.Tests` para `FifaQueryRepository`.

### Task 5 — Regressão e anti-hallucination (AC: 10, 11)

- [ ] 5.1 `dotnet test` nos 4 projetos .NET + `npm test` no backend Node — 0 falhas.
- [ ] 5.2 Revisão anti-hallucination: pacotes/códigos de erro confirmados reais.

---

## Dev Notes

### Onde cada teste novo se encaixa (mapa de lacunas confirmadas nesta story)

| Mecanismo | Hoje | Lacuna | Este AC |
|---|---|---|---|
| Idempotência SQL (`PurchaseRepository.InsertPurchaseAsync`) | Testada só via mock (`PurchaseRepositoryTests.cs`, existente) + e2e manual | Sem teste de integração contra SQL real | AC-2/AC-3 |
| `gatewayTrust.js` (backend Node) | **Zero testes** (confirmado: `fifa2026-api` sem framework de teste) | Toda a lógica de confiança do gateway não tem cobertura automatizada | AC-5/AC-6/AC-7 |
| Guards fail-closed do Gateway (CIAM/Admin config) | Sem teste dedicado (verificado: nenhum arquivo em `Gateway.Tests` testa os 4 `throw`s) | Regressão nesses guards passaria silenciosamente | AC-8 |
| Guards fail-closed de `PurchaseRepository`/`FifaQueryRepository` | Sem teste dedicado | Idem | AC-9 |

### Verificação na fonte da verdade (Art. IV)

| Afirmação | Confirmado em |
|---|---|
| `PurchaseRepository` catch 2627/2601, comentário "NUNCA SELECT-then-INSERT" | `PurchaseRepository.cs` linhas 12-20, 111-119 |
| `fifa2026-api/package.json` sem devDependency de teste | arquivo completo lido (25 linhas) |
| Zero arquivos `.test.js`/`.spec.js`/`__tests__` em `fifa2026-api` | glob (3 padrões) = 0 resultados |
| `gatewayTrust.js` completo (64 linhas) — `safeEqual`, `gatewayTrustMiddleware` | arquivo lido integralmente |
| 4 guards `InvalidOperationException` no Gateway (CIAM/Admin) | `Gateway/Program.cs` linhas 245-293 |
| `Fifa2026.V2.Gateway.Tests` sem arquivo dedicado a guards de startup | listagem de diretório (`AdminProxyTests.cs`, `DualIssuerTests.cs`, `GatewayTestFixture.cs`, `JwtValidationTests.cs`, `OutputCacheTests.cs`, `RateLimitTests.cs`, `RoutingTests.cs`, `TestTokenFactory.cs`) |
| `PurchaseRepository`/`FifaQueryRepository` guard de `SqlConnectionString` | `PurchaseRepository.cs` linhas 22-31; `FifaQueryRepository.cs` linhas 17-28 |
| Nenhum `Testcontainers` no repo hoje | grep `Testcontainers` em `src/` = 0 |

### Testing

**Test file locations:**
- `src/Fifa2026.V2.Functions.Tests/PurchaseRepositoryIntegrationTests.cs` (novo, sugestão de nome)
- `fifa2026-api/src/middleware/__tests__/gatewayTrust.test.js` (novo)
- `src/Fifa2026.V2.Gateway.Tests/StartupGuardTests.cs` (novo, sugestão de nome)
- `src/Fifa2026.V2.Functions.Tests/PurchaseRepositoryTests.cs` (existente — estender com o teste de guard, ou arquivo irmão)
- `src/Fifa2026.V2.McpServer.Tests/` (novo arquivo para o guard de `FifaQueryRepository`)

**Test standards:**
- .NET: xUnit (herdado). Testes de integração isolados por `[Trait("Category","Integration")]`. `WebApplicationFactory` reusa `GatewayTestFixture` já existente para os guards do Gateway.
- Node: Jest (novo — primeira introdução no backend). Padrão simples `describe`/`it`, sem necessidade de supertest (testando as funções `safeEqual`/`gatewayTrustMiddleware` diretamente, com `req`/`res`/`next` mockados manualmente ou via `jest.fn()`).

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-01 | 0.1 | Story drafted — Consolidação-testes (Frente B do EPIC-004). 11 ACs derivados da ADE-009 §Consequences (itens de Consolidação roteados, não invariantes) + plano §5 (itens 2, 4, 6). Lacunas de cobertura confirmadas por inspeção direta (não de memória): `fifa2026-api` sem framework de teste algum; `Gateway.Tests` sem arquivo dedicado aos 4 guards fail-closed; nenhum teste de integração real para a idempotência SQL. Testcontainers.MsSql e Jest identificados como as adições de dependência necessárias (ambos ausentes hoje, confirmado por grep/leitura de package.json). Paralela às Stories 4.1/4.2 (net-new, sem mudança de comportamento). | @sm (River) |
| 2026-07-01 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready. Story net-new de testes, sem mudança de comportamento; gate @qa correto (ADE-009 §Consequences roteou explicitamente esta consolidação como "não invariante"). AC-7 (não medir tempo-constante empiricamente) é limitação honestamente documentada (Art. IV), não defeito. Paralela a 4.1/4.2. | @po (Pax) |

---

## Dev Agent Record

### Agent Model Used

_Pendente — a preencher pelo `@dev` na implementação._

### Completion Notes

_Pendente._

### File List

_Pendente — ver tabela de lacunas em Dev Notes como baseline esperado._

---

## QA Results

_Pendente — gate `@qa` (Quinn), tools: test-design, coverage-analysis, regression-validation._
