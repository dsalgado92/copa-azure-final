# Story 4.2 — Blindar (perímetro interno): `X-Gateway-Key` nas Functions/McpServer (fecha o P0/bypass — a demo-dinheiro) + remover `AllowAllAzureServices` + SQL Private Endpoint (showcase T2)

> **Epic:** [EPIC-004 — "Nível Produção"](../epics/EPIC-004-nivel-producao.md)
> **Status:** Ready
> **Created:** 2026-07-01 by @sm (River)
> **Sprint context:** Story de squad (código + infra), o **P0 do épico** — fecha o bypass das Functions e é a **demo-dinheiro** da aula (`curl` forjando identidade direto na Function → 401; via gateway → 200). Continua na branch `phase-08-hardening` (criada pela Story 4.1) — nenhuma branch nova.
> **Depends on:** Story 4.1 (o `GATEWAY_SHARED_SECRET` que esta story valida no destino já vive no Key Vault, lido pela MI de cada serviço — Inv 1 lê de lá) · [ADE-009](../architecture/ade-009-network-secrets-service-identity.md) Accepted (gate, satisfeito)
> **Blocks:** nenhuma story subsequente diretamente — mas é pré-requisito do **SC-1** (bypass fechado) e do **SC-4** (SQL fechado) do épico, e da demo ao vivo da Final
> **Source (Art. IV — rastreabilidade):** [ADE-009](../architecture/ade-009-network-secrets-service-identity.md) Invariantes 1, 3 (parcial), 4 + §Verificação na fonte da verdade + §Alternatives Considered (Alt 1, Alt 4) · plano `giggly-mapping-hearth.md` §4 · EPIC-004 tabela de Stories (linha S2) e tabela "Como a ADE-009 amarra o escopo" (Inv 1/3/4 → 4.2)
>
> **Nota de numeração (@sm AUTO-DECISION):** Segunda story da série 4.x, sequencial a 4.1.
>
> **Nota de granularidade (@sm AUTO-DECISION):** O épico deixou a critério do `@sm` dividir esta story em 4.2a (`X-Gateway-Key` — o P0) e 4.2b (SQL firewall/PE). **Decisão: manter unificada** — ambas fecham a superfície interna sob o mesmo gate `@architect` (ADE-009 Inv 1 e Inv 4 são irmãs na mesma decisão "endurecer a malha interna"), é o default do épico, e dividir isolaria a demo-dinheiro (Inv 1) de sua correlação direta com o SQL fechado (Inv 4) — ambas fazem parte do MESMO smoke narrativo da aula (plano §10).

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "security-validation"
  - "pattern-validation"
  - "regression-validation"
  - "infra-review"
deployment_owner: "@devops"
```

**Rationale:** Story de código (transform do gateway + validação nova em 2 microsserviços) e infraestrutura (bicep do SQL). `@dev` estende o padrão já provado do gateway (`Gateway/Program.cs:118-144`) e implementa a validação simétrica no destino (Functions/McpServer). `@devops` executa o bicep (firewall + PE/VNet, T2 pré-provisionado fora do relógio da aula — plano §9.4). `@architect` (Aria) é o gate — autora da ADE-009, valida especificamente as Invariantes 1, 3 e 4 (§Validation).

---

## Story

**Como** integrante do squad de desenvolvimento do Living Lab (papel `@dev`, sob o gate `@architect`, com a infraestrutura de rede/firewall de `@devops`),
**eu quero** estender o `X-Gateway-Key` (timing-safe, fail-closed quando configurado) do backend v1 às Functions F1 e ao McpServer — generalizando o transform já escopado por `ClusterId` no gateway e adicionando a validação simétrica no destino — e remover o firewall `AllowAllAzureServices` do Azure SQL (substituindo por acesso restrito via MI-AAD ou Private Endpoint),
**para que** um `curl` forjando `X-Entra-OID` direto na Function receba **401** (hoje recebe 200 — o P0 do bypass), a mesma request via gateway continue recebendo **200**, e o SQL deixe de estar aberto a "qualquer recurso de qualquer tenant Azure" — fechando as Invariantes 1 e 4 da ADE-009 e realizando a demo-dinheiro da aula.

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita a Invariante da ADE-009 ou o trecho de código/infra real verificado. Nenhum AC foi inventado.

### Bloco A — Continuação de branch

1. **AC-1 — Story continua na branch `phase-08-hardening`** (criada pela Story 4.1) — nenhuma branch nova. `@dev`/`@devops` coordenam a ordem de merge com a Story 4.4 (ambas tocam `Gateway/Program.cs`, em regiões de código distintas — o transform de cluster aqui, o pipeline/`UseForwardedHeaders` em 4.4 — sem dependência lógica, só de arquivo compartilhado).
   *(Fonte: Story 4.1 AC-1; EPIC-004 tabela de Stories, linha S2.)*

### Bloco B — `X-Gateway-Key` generalizado no gateway (ADE-009 Inv 1)

2. **AC-2 — Transform `X-Gateway-Key` estendido de `backend-v1` para os clusters `functions-f1` e `mcp-server`:** o `if` que hoje escopa a injeção a um único cluster (`Gateway/Program.cs` linhas 123-126, comparando `transformBuilderContext.Cluster?.ClusterId` a `BackendV1ClusterId`) passa a checar **pertencimento a um conjunto** de 3 cluster IDs — os literais **exatos** verificados em `src/Fifa2026.V2.Gateway/appsettings.json`: `"backend-v1"` (linhas 92/103/114/147), `"functions-f1"` (linhas 41/51/126), `"mcp-server"` (linhas 61/67/133). O cluster `"flow-events"` (linhas 73/82/140) **fica fora** — a ADE-009 Invariante 1 não o menciona (só "v1, Functions F1, McpServer"); nenhuma extensão além do escopo decidido pela ADE (Art. IV).
   *(Fonte: verificado `Gateway/Program.cs` linhas 118-144, especialmente 123-132; `Gateway/appsettings.json` linhas 41-147 — cluster IDs exatos; ADE-009 Inv 1 — "todo backend que o gateway proxia — v1, Functions F1, McpServer".)*

3. **AC-3 — Semântica anti-spoofing preservada exatamente:** o transform continua removendo qualquer `X-Gateway-Key` de origem externa **antes** de (eventualmente) injetar o valor real (`Gateway/Program.cs` linha 132, `transformContext.ProxyRequest.Headers.Remove(GatewayKeyHeader)`) — mesmo padrão já usado para `X-Entra-OID` (linha 97) e para o `backend-v1` atual (linha 132). Só injeta quando `adminSharedSecret` (agora lido via KV reference — Story 4.1 AC-12) está configurado (linha 136, `!string.IsNullOrEmpty`) — vazio = injeção desligada nos 3 clusters (gating idêntico ao já existente para `backend-v1`).
   *(Fonte: verificado `Gateway/Program.cs` linhas 128-143.)*

4. **AC-4 — `[AUTO-DECISION]` Um único `GATEWAY_SHARED_SECRET` compartilhado entre os 3 clusters (não segredos distintos por serviço):** ver §"Decisão de design" abaixo.

### Bloco C — Validação no destino: Functions F1 (ADE-009 Inv 1)

5. **AC-5 — `PurchaseEntryFunction` e `PurchaseStatusFunction` validam `X-Gateway-Key` antes de processar a request** (ambas hoje `AuthorizationLevel.Anonymous` — verificado `PurchaseEntryFunction.cs` linha 58, `PurchaseStatusFunction.cs` linha 27): uma validação nova, aplicada às invocações **HTTP-triggered** do worker isolado (via `IFunctionsWorkerApplicationBuilder.UseMiddleware<T>()` registrado em `ConfigureFunctionsWebApplication`, em `Program.cs` — mecanismo documentado nativo do Azure Functions Isolated Worker, não inventado; **não** se aplica à invocação Service Bus-triggered de `PurchaseConsumerFunction`, que não tem headers HTTP), com a **mesma semântica** de `gatewayTrust.js` (`fifa2026-api/src/middleware/gatewayTrust.js`, verificado linhas 28-38/44-61): comparação **timing-safe** (`CryptographicOperations.FixedTimeEquals` em .NET — equivalente ao `crypto.timingSafeEqual` do Node), **fail-closed** quando `GATEWAY_SHARED_SECRET` está configurado (header ausente/divergente → **401**), **legado/passa-through** quando o segredo está vazio (preserva o comportamento `Anonymous` atual — compatibilidade retroativa com Oitavas/F1, que não têm gateway).
   *(Fonte: verificado `PurchaseEntryFunction.cs` linha 58, `PurchaseStatusFunction.cs` linha 27; `gatewayTrust.js` linhas 28-38, 44-61 — padrão a generalizar; ADE-009 Inv 1 — "(a) Trava de aplicação X-Gateway-Key... comparação de tempo constante... fail-closed quando o segredo está configurado... legado quando o segredo está vazio".)*

6. **AC-6 — Fecha o P0 (a demo-dinheiro):** com o segredo configurado, um `curl` que forje `X-Entra-OID` **direto na URL da Function** (sem passar pelo gateway, portanto sem `X-Gateway-Key` válido) recebe **401**. A **mesma** request, feita através do gateway (que injeta o `X-Gateway-Key` real via o transform da AC-2), recebe **200** — comportamento idêntico ao de hoje. Este é o smoke central da Story (§Validation ADE-009, item 1) e a demo ao vivo da aula.
   *(Fonte: ADE-009 Inv 1 — "Fecha o P0 (o bypass das Functions)"; ADE-009 §Validation item 1.)*

### Bloco D — Validação no destino: McpServer (ADE-009 Inv 1)

7. **AC-7 — McpServer valida `X-Gateway-Key` antes de `MapMcp("/mcp")` processar a request:** um novo middleware ASP.NET Core (mesmo estilo estrutural de `XCacheMiddleware.cs` do gateway — classe própria em `Fifa2026.V2.McpServer.Infrastructure`, registrado via `app.UseMiddleware<T>()` **antes** de `app.MapMcp("/mcp")` em `Program.cs`), com a **mesma semântica timing-safe/fail-closed-quando-configurado/legado-quando-vazio** da AC-5. Fecha a lacuna que a ADE-009 aponta: "não há IaC versionado garantindo `ingress: internal`" para o McpServer (verificado: nenhum YAML de IaC existe para ele, `infra/**/*.yaml` = só `n8n`/`flow-events`) — o `X-Gateway-Key` é o **piso enforçável em código**, independente de o isolamento de rede estar ou não versionado.
   *(Fonte: verificado `src/Fifa2026.V2.McpServer/Program.cs` linhas 19-22 (McpServer nunca revalida JWT, confia no gateway); ADE-009 Inv 1 — "Por que o McpServer também, se ele 'está atrás do gateway'".)*

8. **AC-8 — `/health` do McpServer e do Functions NÃO exige `X-Gateway-Key`:** os health probes (`app.MapGet("/health", ...)` do McpServer, verificado linha 69; e qualquer endpoint de saúde equivalente das Functions, se existir) permanecem acessíveis sem o header — a validação se aplica só às rotas de negócio (`v2/purchase`, `v2/purchase/{correlationId}`, `/mcp`), para não quebrar health checks do Container Apps Environment/Function App.
   *(Fonte: verificado `McpServer/Program.cs` linha 69; boa prática de plataforma — health probes não devem exigir segredo de aplicação.)*

### Bloco E — SQL: remover `AllowAllAzureServices` (ADE-009 Inv 4)

9. **AC-9 — Firewall `AllowAllAzureServices` removido de `sql-database.bicep`:** o recurso `fwAllowAzure` (`0.0.0.0`–`0.0.0.0`, verificado linhas 60-67) é **removido** por padrão (T1) — não é "os serviços do lab", é qualquer recurso de qualquer tenant Azure (comentário do próprio bicep já sinaliza isso, linha 58-59: "Para apertar: trocar por Private Endpoint + VNet integration"). Acesso à base passa a depender **exclusivamente** de autenticação MI-AAD (Story 4.1) com identidades nomeadas de menor-privilégio — "nem há senha" nas conexões de aplicação (ADE-009 Alt 4, rejeitada: "manter `AllowAllAzureServices` confiando na força da senha").
   *(Fonte: verificado `sql-database.bicep` linhas 58-67; ADE-009 Inv 4 — "remove `AllowAllAzureServices`"; Alt 4 rejeitada.)*

10. **AC-10 — `publicNetworkAccess` parametrizado para as duas trilhas T1/T2:** `sql-database.bicep` ganha um parâmetro (ex.: `enablePrivateEndpoint bool = false`) que, quando `false` (T1, padrão — **US$0**), mantém `publicNetworkAccess: 'Enabled'` (necessário para conectividade sem VNet — Container Apps em Consumption não têm egress IP estável, "a confirmar"/nota operacional) SEM a regra wildcard (AC-9) — a superfície de acesso passa a ser "alcançável na rede, mas só autentica quem tem token AAD válido para uma identidade com grant". Quando `true` (T2 — showcase ~US$7/mês), adiciona um **Private Endpoint** + `publicNetworkAccess: 'Disabled'` — mesmo arquivo, mesma fonte da verdade, sem duplicar o módulo.
    *(Fonte: ADE-009 §Priorização T1/T2/T3 tabela — "T1: MI-AAD com publicNetworkAccess restrito (não wildcard)... T2: SQL Private Endpoint + publicNetworkAccess Disabled"; ADE-009 Inv 4.)*

11. **AC-11 — T2 (Private Endpoint) é pré-provisionado pelo instrutor, fora do relógio da aula** (plano §9.4 "proteger o clímax"; ADE-009 Alt 5 rejeitada — "construir o T3/rede pleno como a aula"): a VNet + o CAE VNet-integrado + o Private Endpoint do SQL sobem via `browser-harness` (ferramenta de instrutor, offline — Story 4.5) **antes** da sessão; o aluno não provisiona rede ao vivo. Ordem dura (ADE-009 Inv 3, herdada das Quartas): **VNet+CAE nascem primeiro** (CAE é imutável — recriar muda o FQDN do gateway, forçando rebuild do frontend); o **Private Endpoint do SQL** é criado depois, sobre a VNet já existente.
    *(Fonte: ADE-009 Inv 3 + Alt 5; plano §9.4; aprendizado registrado nas Quartas — recriar a VNet do env em PRD mudou o FQDN do gateway.)*

### Bloco F — Regressão e anti-hallucination

12. **AC-12 — Compatibilidade retroativa preservada (labs sem gateway):** com `GATEWAY_SHARED_SECRET` vazio/ausente, as Functions e o McpServer continuam se comportando exatamente como hoje (`Anonymous`, sem exigência de `X-Gateway-Key`) — os labs das Oitavas (F1, sem gateway) e qualquer ambiente de desenvolvimento local sem o segredo configurado **não quebram**. Nenhum branch de código por ambiente — o gating é só pela presença/ausência do valor de configuração (idêntico ao padrão já usado pelo `backend-v1`).
    *(Fonte: ADE-009 Inv 1 — "compatibilidade retroativa (gating por segredo vazio)"; EPIC-004 Restrições firmes.)*

13. **AC-13 — ADE-004 preservada: gateway continua o único validador de JWT.** Nem `PurchaseEntryFunction`/`PurchaseStatusFunction` nem o McpServer passam a validar o token Entra — eles só passam a **provar que a request veio do gateway** (via `X-Gateway-Key`), exatamente como o `gatewayTrust.js` já faz para `backend-v1`. `X-Entra-OID` continua sendo lido sem validação própria (linhas 129-134 de `PurchaseEntryFunction.cs`, intocadas por esta story).
    *(Fonte: ADE-009 Inv 1 — "o gateway permanece o guardião único do JWT"; verificado `PurchaseEntryFunction.cs` linhas 129-134 fora de escopo desta AC.)*

14. **AC-14 — `grep` de regressão de segurança (completa, junto com a Story 4.1):** `grep` por `AllowAllAzureServices`/`0.0.0.0` em `infra/modules/sql-database.bicep` = **0** ocorrências pós-merge. Combinado com o `grep` de connection strings da Story 4.1 AC-15, satisfaz o EPIC-004 SC-2/SC-4 por completo.
    *(Fonte: EPIC-004 SC-4 "AllowAllAzureServices removido"; verificado `sql-database.bicep` linhas 60-67.)*

15. **AC-15 — Anti-hallucination (Art. IV):** `CryptographicOperations.FixedTimeEquals` (.NET, comparação em tempo constante — API real do namespace `System.Security.Cryptography`), `IFunctionsWorkerApplicationBuilder.UseMiddleware<T>()` (mecanismo real de middleware do Azure Functions Isolated Worker), e os cluster IDs (`functions-f1`, `mcp-server`, `backend-v1`) são literais **exatos** verificados em `appsettings.json` — nenhum nome inventado. O padrão de middleware do McpServer (AC-7) reusa a estrutura de `XCacheMiddleware.cs` já existente no repo, não introduz um framework novo.

---

## `[AUTO-DECISION]` — a pendência de design da Invariante 1, resolvida aqui

### Um `GATEWAY_SHARED_SECRET` compartilhado entre `backend-v1`/`functions-f1`/`mcp-server` → **SIM, um único segredo**

**Decisão:** o mesmo valor de `GATEWAY_SHARED_SECRET` (vindo do Key Vault, Story 4.1) é injetado pelo gateway nos 3 clusters, e cada backend o compara contra sua própria cópia do mesmo valor (lido também via KV reference).

**Por quê (justificativa):**
- **A própria ADE-009 trata "X-Gateway-Key" como um conceito único, não per-serviço.** O texto fala de "o segredo" (singular) em toda a Invariante 1, e a §Consequences discute explicitamente o blast radius de UM segredo compartilhado: *"X-Gateway-Key é segredo simétrico compartilhado... Blast radius contido: se vazar, rota-se; não dá acesso a dados por si só (ainda exige alcançar a rede)"* — a ADE já aceitou esse trade-off.
- **Reúso do padrão exato das Quartas.** `gatewayTrust.js` já valida contra `GATEWAY_SHARED_SECRET` (singular, sem sufixo de serviço) — estender o MESMO nome/valor aos 2 novos destinos é generalização literal, não uma nova decisão de segurança.
- **Menor cerimônia operacional.** Um segredo no Key Vault (Story 4.1) é mais simples de rotacionar/auditar que três — e a ADE-009 já estabeleceu que o Key Vault é o lar dos irredutíveis, sem distinguir "um por serviço".
- **Alternativa registrada (não escolhida):** segredos distintos por cluster (`GATEWAY_SHARED_SECRET_FUNCTIONS`, `GATEWAY_SHARED_SECRET_MCP`) isolariam o blast radius por serviço — mais forte, mas não pedido pela ADE-009 e adiciona 2x a cerimônia de KV/rotação sem invariante que o exija. Fica como refinamento futuro (T3-like), não desta story.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Security (fecha um bypass de autenticação real — P0)
**Secondary Type(s):** Architecture (generaliza um transform já provado), Deployment (bicep do SQL — firewall/PE)
**Complexity:** HIGH (toca o pipeline de autorização de 3 serviços simultaneamente; erro de gating quebraria compras em produção OU deixaria o bypass aberto — as duas falhas são inaceitáveis; a mudança no SQL firewall tem efeito imediato em conectividade se a MI da Story 4.1 não estiver pronta)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — transform do gateway + middlewares novos em Functions/McpServer
- @devops (Gage) — bicep do firewall/PE, pré-provisionamento T2

**Supporting Agents:**
- @architect (Aria) — quality gate (ADE-009 Inv 1/3/4)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco no gating fail-closed/fail-open (não pode haver caminho onde um segredo configurado seja ignorado, nem onde a ausência de segredo quebre labs sem gateway)
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — regressão (Oitavas/F1 sem gateway continuam funcionando)
- [ ] Pre-Deployment (@devops): smoke ao vivo do bypass (401 direto / 200 via gateway) **antes** de remover `AllowAllAzureServices` em produção — ordem de deploy importa (Story 4.1 MI deve estar propagada primeiro)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (bloqueia se persistir — P0 de segurança não tolera debt)
- HIGH issues: auto_fix (documenta como debt se falhar)

### CodeRabbit Focus Areas

**Primary Focus:**
- Comparação timing-safe real (`CryptographicOperations.FixedTimeEquals`, não `==`/`string.Equals` — vulnerável a timing attack)
- Gating fail-closed correto: segredo configurado + header ausente/divergente → 401 SEMPRE; segredo vazio → passa-through SEMPRE (nenhum caminho intermediário)
- Anti-spoofing: `X-Gateway-Key` de origem externa é sempre removido antes da injeção (regressão do padrão já existente em `backend-v1`)

**Secondary Focus:**
- Middleware das Functions não intercepta a invocação Service Bus-triggered (`PurchaseConsumerFunction`) — só HTTP
- `/health` sem exigência de `X-Gateway-Key`
- Ordem de deploy: MI (4.1) propagada antes de remover `AllowAllAzureServices` (evita lockout)

---

## Tasks / Subtasks

### Task 1 — Branch (AC: 1)

- [ ] 1.1 Confirmar que o trabalho continua em `phase-08-hardening` (criada pela Story 4.1). Coordenar com `@devops`/`@dev` da Story 4.4 a ordem de merge (ambas tocam `Gateway/Program.cs`).

### Task 2 — Gateway: generalizar o transform (AC: 2, 3, 4)

- [ ] 2.1 Substituir o `if (string.Equals(ClusterId, BackendV1ClusterId, ...))` (linha ~123) por um `HashSet<string>` (ou `switch`) contendo `"backend-v1"`, `"functions-f1"`, `"mcp-server"` — `OrdinalIgnoreCase`.
- [ ] 2.2 Confirmar que a remoção do header externo (anti-spoofing, linha 132) e o gating por `adminSharedSecret` não-vazio (linha 136) permanecem intocados na nova condição.

### Task 3 — Functions: middleware de validação (AC: 5, 6, 8)

- [ ] 3.1 Criar middleware isolated-worker (ex.: `GatewayKeyValidationMiddleware`) em `Fifa2026.V2.Functions`, registrado via `builder.UseMiddleware<T>()` dentro de `ConfigureFunctionsWebApplication` em `Program.cs`.
- [ ] 3.2 Middleware aplica a validação SÓ em invocações HTTP-triggered (checar a presença de `HttpRequestData`/feature HTTP no `FunctionContext`); passa direto para invocações Service Bus-triggered.
- [ ] 3.3 Implementar comparação timing-safe (`CryptographicOperations.FixedTimeEquals`) contra o `GATEWAY_SHARED_SECRET` (App Setting, agora via KV reference da Story 4.1); fail-closed se configurado e ausente/divergente (401); passa direto se vazio.
- [ ] 3.4 Confirmar que `/health` (se existir endpoint equivalente nas Functions) não passa pelo middleware, ou é explicitamente excluído.
- [ ] 3.5 Smoke (runtime/@devops): `curl` direto na Function com `X-Entra-OID` forjado → 401; via gateway → 200.

### Task 4 — McpServer: middleware de validação (AC: 7, 8)

- [ ] 4.1 Criar `GatewayKeyValidationMiddleware` (ASP.NET Core, mesmo estilo estrutural de `XCacheMiddleware.cs`) em `Fifa2026.V2.McpServer/Infrastructure/`.
- [ ] 4.2 Registrar `app.UseMiddleware<T>()` **antes** de `app.MapMcp("/mcp")` em `Program.cs`.
- [ ] 4.3 Mesma semântica timing-safe/fail-closed-quando-configurado/legado-quando-vazio da Task 3.3.
- [ ] 4.4 Confirmar que `app.MapGet("/health", ...)` (linha 69) não exige o header.

### Task 5 — SQL: firewall + PE (AC: 9, 10, 11)

- [ ] 5.1 Remover o recurso `fwAllowAzure` (`AllowAllAzureServices`) de `sql-database.bicep` por padrão.
- [ ] 5.2 Adicionar parâmetro `enablePrivateEndpoint bool = false` — quando `true`, adiciona recurso(s) de Private Endpoint + seta `publicNetworkAccess: 'Disabled'`.
- [ ] 5.3 (@devops, runtime) Pré-provisionar T2 (VNet + CAE VNet-integrado + PE) via `browser-harness`, fora do relógio da aula, respeitando a ordem VNet+CAE→FQDN→`VITE_*`/CORS (ADE-009 Inv 3).

### Task 6 — Regressão e anti-hallucination (AC: 12, 13, 14, 15)

- [ ] 6.1 `dotnet build` + `dotnet test` em `Fifa2026.V2.Functions.Tests`, `Fifa2026.V2.McpServer.Tests`, `Fifa2026.V2.Gateway.Tests` — 0 erros.
- [ ] 6.2 Smoke de compatibilidade: com `GATEWAY_SHARED_SECRET` vazio, Functions/McpServer respondem exatamente como hoje (sem exigir header).
- [ ] 6.3 `grep` de regressão: `AllowAllAzureServices`/`0.0.0.0` em `sql-database.bicep` = 0.
- [ ] 6.4 Revisão anti-hallucination: `CryptographicOperations.FixedTimeEquals`, cluster IDs, mecanismo de middleware — todos confirmados reais.

---

## Dev Notes

### Arquitetura técnica delta

```
[Gateway/Program.cs linha ~123]
  ANTES: if (ClusterId == "backend-v1") { inject X-Gateway-Key }
  DEPOIS: if (ClusterId in {"backend-v1","functions-f1","mcp-server"}) { inject X-Gateway-Key }
  (mesmo adminSharedSecret, mesma anti-spoofing, mesmo gating por segredo vazio)

[Fifa2026.V2.Functions/Program.cs]
  + UseMiddleware<GatewayKeyValidationMiddleware>() — SÓ HTTP-triggered
  (PurchaseEntryFunction/PurchaseStatusFunction ganham a trava; PurchaseConsumerFunction NÃO,
   pois é Service Bus-triggered, sem headers HTTP)

[Fifa2026.V2.McpServer/Program.cs]
  + UseMiddleware<GatewayKeyValidationMiddleware>() ANTES de MapMcp("/mcp")
  (mesma semântica; /health fora do escopo)

[sql-database.bicep]
  - resource fwAllowAzure (AllowAllAzureServices) REMOVIDO por padrão
  + param enablePrivateEndpoint bool = false
  + (se true) Private Endpoint + publicNetworkAccess: 'Disabled'
```

### Arquivos tocados (File List esperada)

| Arquivo | Ação |
|---|---|
| `src/Fifa2026.V2.Gateway/Program.cs` | Modificado (transform generalizado para 3 clusters) |
| `src/Fifa2026.V2.Functions/Infrastructure/GatewayKeyValidationMiddleware.cs` (nome sugerido) | **Novo** |
| `src/Fifa2026.V2.Functions/Program.cs` | Modificado (registro do middleware) |
| `src/Fifa2026.V2.McpServer/Infrastructure/GatewayKeyValidationMiddleware.cs` (nome sugerido) | **Novo** |
| `src/Fifa2026.V2.McpServer/Program.cs` | Modificado (registro do middleware, antes de `MapMcp`) |
| `infra/modules/sql-database.bicep` | Modificado (remove wildcard; + param PE) |
| `src/Fifa2026.V2.Gateway.Tests/AdminProxyTests.cs` ou novo arquivo | Modificado/Novo (cobertura dos 2 novos clusters) |

**NÃO tocam (fora de escopo, ADEs preservadas):** `PurchaseConsumerFunction.cs` (Service Bus-triggered, sem HTTP); `Gateway/Program.cs` pipeline de auth/cache (linhas 419-424 — é a Story 4.4); lógica de leitura de `X-Entra-OID` (linhas 94-116, 129-134 — ADE-007 preservada); `FifaQueryRepository.cs`/`PurchaseRepository.cs` (autenticação SQL é a Story 4.1, já concluída como pré-requisito).

### Verificação na fonte da verdade (Art. IV)

| Afirmação | Confirmado em |
|---|---|
| Transform `X-Gateway-Key` escopado só a `backend-v1` | `Gateway/Program.cs` linhas 118-144, esp. 123-132 |
| Cluster IDs exatos: `backend-v1`, `functions-f1`, `mcp-server`, `flow-events` | `Gateway/appsettings.json` linhas 41-147 |
| `PurchaseEntryFunction`/`PurchaseStatusFunction` = `AuthorizationLevel.Anonymous` | `PurchaseEntryFunction.cs` linha 58; `PurchaseStatusFunction.cs` linha 27 |
| `gatewayTrust.js` timing-safe + fail-closed condicionado | linhas 28-38, 44-61 |
| McpServer nunca revalida JWT, confia no gateway | `McpServer/Program.cs` linhas 19-22 |
| `/health` do McpServer sem auth | `McpServer/Program.cs` linha 69 |
| Nenhum IaC de `ingress: internal` para o McpServer | glob `infra/**/*.yaml` = 2 arquivos (n8n, flow-events) |
| `AllowAllAzureServices` `0.0.0.0`–`0.0.0.0` + comentários apontando PE | `sql-database.bicep` linhas 58-67 |
| `XCacheMiddleware.cs` como padrão estrutural de middleware do gateway a espelhar | arquivo completo, `src/Fifa2026.V2.Gateway/Infrastructure/XCacheMiddleware.cs` |

### Troubleshooting esperado

| Sintoma | Causa | Mitigação |
|---|---|---|
| Compra passa a falhar com 401 mesmo via gateway | Gateway e Function com valores DIFERENTES de `GATEWAY_SHARED_SECRET` (dessincronia KV) | Confirmar que ambos leem a MESMA KV reference/valor (Story 4.1 AC-12) |
| Labs antigos (Oitavas/F1) começam a exigir header | Segredo foi configurado sem querer no ambiente do lab antigo | Confirmar `GATEWAY_SHARED_SECRET` vazio nos ambientes sem gateway (gating por config, não por código) |
| SQL fica inacessível após remover `AllowAllAzureServices` | MI da Story 4.1 não propagou RBAC/contained user ainda, ou `publicNetworkAccess` ficou `Disabled` sem PE pronto (T1 esperava `Enabled`) | Sequenciar: 4.1 completa e validada ANTES de remover o wildcard em produção; confirmar `enablePrivateEndpoint` bate com o tier pretendido |
| `curl` direto na Function ainda dá 200 (P0 não fechou) | Middleware não registrado corretamente, ou intercepta só parte das rotas | Confirmar `UseMiddleware<T>()` no pipeline do worker isolado e que cobre `v2/purchase` E `v2/purchase/{correlationId}` |

### Testing

**Test file locations:**
- `src/Fifa2026.V2.Gateway.Tests/AdminProxyTests.cs` (existente — estender ou criar arquivo irmão para `functions-f1`/`mcp-server`)
- `src/Fifa2026.V2.Functions.Tests/` (novo arquivo, ex.: `GatewayKeyValidationMiddlewareTests.cs`)
- `src/Fifa2026.V2.McpServer.Tests/` (novo arquivo, ex.: `GatewayKeyValidationMiddlewareTests.cs`)

**Test standards:**
- xUnit (herdado). Padrão de teste do transform do gateway: `WebApplicationFactory`/`TestServer` (herdado de `Gateway.Tests`), confirmando header presente com secret configurado e ausente com secret vazio.
- Unit test do middleware novo (Functions/McpServer): timing-safe compare testado com valores corretos/incorretos/tamanhos diferentes (espelha a cobertura que a Story 4.3 formaliza para `gatewayTrust.js` no lado Node).
- Cobertura de integração completa (`gatewayTrust.js`, startup guards) é a **Story 4.3** — esta story garante só que o novo código não regride o build/testes existentes.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-01 | 0.1 | Story drafted — o P0/demo-dinheiro do EPIC-004 (Missão Blindar, Frente A). 15 ACs derivados da ADE-009 Inv 1/3/4, verificados contra `Gateway/Program.cs` (transform + cluster IDs em `appsettings.json`), `gatewayTrust.js`, `PurchaseEntryFunction.cs`/`PurchaseStatusFunction.cs` (`AuthorizationLevel.Anonymous`), `McpServer/Program.cs`, `sql-database.bicep`. Resolvida 1 decisão de design (`[AUTO-DECISION]`): segredo único compartilhado entre os 3 clusters (não segredos distintos por serviço) — justificado pelo próprio texto da ADE-009 sobre blast radius aceito. Continua na branch `phase-08-hardening` (Story 4.1); coordenação de merge sinalizada com a Story 4.4 (mesmo arquivo `Gateway/Program.cs`, regiões distintas). | @sm (River) |
| 2026-07-01 | 0.1.1 | Validated GO (10/10) — Status: Draft → Ready. AC-4 [AUTO-DECISION] (segredo `GATEWAY_SHARED_SECRET` único compartilhado entre os 3 clusters) confirmado pelo @po contra a ADE-009 §Consequences — o blast radius de UM segredo simétrico compartilhado é explicitamente aceito pela ADE (irredutível→KV, rotação simples). Coordenação de merge com 4.4 (mesmo `Gateway/Program.cs`, regiões distintas) declarada. | @po (Pax) |

---

## Dev Agent Record

### Agent Model Used

_Pendente — a preencher pelo `@dev` na implementação._

### Completion Notes

_Pendente._

### File List

_Pendente — ver tabela "Arquivos tocados" em Dev Notes como baseline esperado._

---

## QA Results

_Pendente — gate `@architect` (Aria), tools: security-validation, pattern-validation, regression-validation, infra-review. Aplicar o §Validation da ADE-009 (itens de Inv 1/3/4) como checklist — o item 1 ("bypass fechado") é o mais crítico desta story._
