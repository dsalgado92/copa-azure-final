# Story 3.1 — Re-arquitetura ADE-008 (código): pós-compra inline + F6 6→5 nós + remoção do código n8n + doc-fix M-1

> **Epic:** [EPIC-003 — "A Final"](../epics/EPIC-003-final-lab.md)
> **Status:** Ready
> **Created:** 2026-06-30 by @sm (River)
> **Sprint context:** Story de re-arquitetura (código), **linchpin** do épico da Final. Não é lab de aluno — é o trabalho de squad que realiza a ADE-008 no código ANTES de qualquer lab (3.3/3.4) poder ser empacotado. Toca 3 projetos (.NET Functions, McpServer, FlowEvents) + 2 arquivos de frontend.
> **Depends on:** EPIC-002 Quartas (Story 2.11) Done · [ADE-008](../architecture/ade-008-final-decommission-n8n.md) Accepted (gate do épico, já satisfeito)
> **Blocks:** Story 3.2 (descomissionamento de infra só depois do código parar de referenciar n8n), Story 3.3 (F5 lab exige McpServer só-sentidos), Story 3.4 (F6 lab exige o enum de 5 nós)
> **Source (Art. IV — rastreabilidade):** ADE-008 Invariantes 1, 2, 3, 4, 5, 6 · ADE-000 Inv 4 (idempotência) e Inv 5 (correlationId) · decisões do owner 2026-06-30 (EPIC-003 §Decisões fechadas #2, #5, #6, #7) · verificado linha-a-linha no código real (ver §Verificação abaixo)
>
> **Nota de numeração (@sm AUTO-DECISION):** Primeira story livre da série 3.x (nenhuma existe ainda). Numeração confirmada por `@po` na validação.
>
> **Nota de granularidade (@sm AUTO-DECISION):** O épico deixou a critério do `@sm` dividir esta story em 3.1a (Functions)/3.1b (F6) se o tamanho incomodar. **Decisão: manter unificada**, como o épico já recomenda por default — é **uma única mudança arquitetural** (a ADE-008), com um único gate `@architect`, e as 3 frentes (pós-compra inline, F6 5 nós, limpeza McpServer) compartilham a mesma causa raiz (remover o n8n) e a mesma prova de regressão (grep `N8N_`/`n8n` = 0). Dividir criaria 3 gates redundantes sobre a mesma decisão.

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "code-review"
  - "pattern-validation"
  - "architecture-review"
  - "regression-validation"
deployment_owner: "@devops"
```

**Rationale:** Story 100% de código (.NET + TypeScript), sem trabalho de infraestrutura Azure (isso é 3.2) nem de conteúdo didático (isso é 3.3/3.4/3.6). `@dev` implementa as 3 frentes (Functions, McpServer, FlowEvents+front). `@architect` (Aria) é o gate natural — é quem escreveu a ADE-008 e quem valida se a remoção de componente foi completa e se a regra de ouro permanece trivialmente verdadeira (§Validation da ADE-008, 8 itens, a maioria cai sobre esta story). `@devops` só entra para confirmar o slot da branch nova e coordenar o push — não há deploy Azure nesta story (isso é 3.2).

---

## Story

**Como** integrante do squad de desenvolvimento do Living Lab (papel `@dev`, sob o gate `@architect`),
**eu quero** substituir a chamada ao webhook n8n na `PurchaseConsumerFunction` por uma notificação inline com trace correlacionado, remover o nó `N8N_WEBHOOK_TRIGGERED` do Flow Visualizer (F6: 6 → 5 nós) e aposentar todo o código legado do n8n (nos projetos `Fifa2026.V2.Functions` e `Fifa2026.V2.McpServer`),
**para que** a arquitetura da Final rode **sem orquestração externa** — preservando a idempotência (ADE-000 Inv 4: notifica só em `Inserted`) e a rastreabilidade por `correlationId` (ADE-000 Inv 5) — e a regra de ouro do chatbot ("nunca escreve direto no banco") passe a valer **por construção**, não por roteamento, conforme ADE-008.

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita a invariante da ADE-008 ou o trecho de código real que fundamenta a mudança. Nenhum AC foi inventado.

### Bloco A — Branch (ADE-008 §Impact + EPIC-003 tabela de Stories)

1. **AC-1 — Branch `phase-07-final-rearch` criada** a partir de **`phase-04-quartas`** (branch atual, estado cumulativo pós-Quartas — confirmado: `phase-04-quartas` NÃO está mergeada em `main`, então é a base correta para não perder F1-F6/McpServer/FlowEvents). Nome e slot final confirmados por `@devops` no push.
   *(Fonte: EPIC-003 tabela de Stories, linha S1; padrão herdado — cada fase branch a partir da anterior, ex.: 2.11 partiu de `phase-03-identity`.)*

### Bloco B — Pós-compra inline na `PurchaseConsumerFunction` (ADE-008 Inv 3; ADE-000 Inv 4/5)

2. **AC-2 — `PurchaseConsumerFunction` não referencia mais `IN8nWebhookNotifier`:** o campo `_n8nNotifier` e o parâmetro correspondente do construtor são removidos. A notificação pós-compra passa a ser **inline**, dentro do mesmo bloco `case InsertOutcome.Inserted:` onde hoje está a chamada `_n8nNotifier.NotifyPurchaseAsync(...)` (verificado: `PurchaseConsumerFunction.cs`, linhas 83-102) — **nunca** em `Duplicate` nem `CategoryNotFound` (idempotência preservada, ADE-000 Inv 4).
   *(Fonte: ADE-008 Inv 3 + §Validation item 1.)*

3. **AC-3 — Forma da notificação inline = log estruturado** (`ILogger.LogInformation`), **sem chamada HTTP** a nenhum endpoint (mock ou real). `[AUTO-DECISION]` **HTTP mock vs só log → LOG** (ver §Decisões de design abaixo — ambas as formas honram a decisão do owner "mock HTTP/log"; log é a mais simples e elimina a última dependência de rede/configuração do pós-compra).
   *(Fonte: ADE-008 Inv 3 — "forma concreta a confirmar no draft da story; ambas honram a decisão do owner".)*

4. **AC-4 — `correlationId` da notificação inline vem do `BeginScope` já existente** (linha 62 do arquivo — `_logger.BeginScope(new Dictionary<string, object> { ["CorrelationId"] = message.CorrelationId })`) — nenhuma nova fonte de correlação é introduzida. A notificação inline HERDA a correlação automaticamente por estar dentro do `using` já aberto.
   *(Fonte: ADE-008 Inv 3 — "o consumer continua emitindo o rastro correlacionado via BeginScope"; M-1.)*

5. **AC-5 — Idempotência regressiva confirmada:** a notificação dispara **apenas** quando `InsertOutcome.Inserted`; permanece **silenciosa** em `Duplicate` (reentrega do Service Bus) e em `CategoryNotFound` (falha permanente → DLQ). Comportamento idêntico ao herdado de F4 (Story 2.4 AC-6/AC-7), agora sem n8n.
   *(Fonte: ADE-000 Inv 4; teste herdado `Webhook_n8n_is_NOT_fired_on_Duplicate`.)*

### Bloco C — F6 Flow Visualizer: 6 → 5 nós (ADE-008 Inv 5)

6. **AC-6 — `FlowEventType` (backend) sem `N8N_WEBHOOK_TRIGGERED`:** o membro `N8N_WEBHOOK_TRIGGERED = 4` é removido; `SQL_INSERTED` é renumerado de ordinal `5` para `4`. Os ordinais 0-3 (`GATEWAY_YARP_RECEIVED`, `FUNCTION_ENTRY_PROCESSED`, `SERVICE_BUS_PUBLISHED`, `FUNCTION_CONSUMER_DONE`) **não mudam**.
   *(Fonte: ADE-008 Inv 5 + §Validation item 4; verificado `FlowEventType.cs` linhas 11-30.)*

7. **AC-7 — `TraceEventMapper.Classify` sem o branch de classificação n8n:** o `if (msg.Contains("webhook n8n") || msg.Contains("n8n") || role.Contains("n8n")) return FlowEventType.N8N_WEBHOOK_TRIGGERED;` (hoje linhas 56-60) é removido. A ordem de avaliação dos branches remanescentes é preservada (o comentário original "avaliado antes do consumer pois 'n8n' é mais específico" deixa de fazer sentido sem o branch n8n — `@dev` pode simplificar a ordem, documentando a mudança).
   *(Fonte: ADE-008 Inv 5 + §Validation item 4; verificado `TraceEventMapper.cs` linhas 56-60.)*

8. **AC-8 — Frontend com 5 nós:** `flowNodes.ts` (`FLOW_NODES`) sem a entrada `index: 4, eventType: 'N8N_WEBHOOK_TRIGGERED'`; a entrada `SQL` passa a `index: 4`. `FlowDiagram.tsx` atualizado (comentário de cabeçalho "6 nós" → "5 nós"; `aria-label="Fluxo da compra: 5 nós..."`; grid `lg:grid-cols-6` → `lg:grid-cols-5` se aplicável ao layout de 5 colunas).
   *(Fonte: ADE-008 Inv 5 — "ajustar o FlowDiagram do front (6 → 5 nós)"; verificado `flowNodes.ts` linhas 30-96, `FlowDiagram.tsx` linhas 1-56.)*

9. **AC-9 — Smoke: bolinha atravessa exatamente 5 nós em < 30s** (Gateway YARP → Function Entry → Service Bus → Function Consumer → SQL). Este AC **supersede funcionalmente** o AC-7 da Story 2.6 (que falava em "6 nós") — a Story 2.6 permanece **Done e intocada** (registro histórico do que foi construído naquela época); esta story (3.1) é a fonte de verdade ATUAL do smoke test do F6.
   *(Fonte: ADE-008 Inv 5 — "AC-7 do smoke = 'bolinha atravessa 5 nós', não 6".)*

### Bloco D — Limpeza do código legado n8n: regra de ouro trivial (ADE-008 Inv 1, 2, 4)

10. **AC-10 — `Fifa2026.V2.Functions` sem código n8n:** `N8nWebhookNotifier.cs`, `IN8nWebhookNotifier.cs` e `N8nWebhookPayload.cs` removidos; `Program.cs` sem o registro `services.AddHttpClient<IN8nWebhookNotifier, N8nWebhookNotifier>(...)` (linhas 26-29); `local.settings.json` sem a chave `N8N_WEBHOOK_URL` (linhas 18-19).
    *(Fonte: ADE-008 §Impact + §Validation item 6; verificado `Program.cs` e `local.settings.json`.)*

11. **AC-11 — `Fifa2026.V2.McpServer` sem a "mão" e sem código n8n:** `FifaActionTools.cs` (tool `criar_alerta_ingresso`), `AlertWebhookNotifier.cs`, `IAlertWebhookNotifier.cs` e `AlertPayloads.cs` removidos; `Program.cs` sem `services.AddHttpClient(AlertWebhookNotifier.HttpClientName, ...)` nem `services.AddScoped<IAlertWebhookNotifier, AlertWebhookNotifier>()` (linhas 47-48). Resultado: **`tools/list` volta a listar exatamente 7 tools** (as da Fase A / Story 2.8), **todas `ReadOnly = true`** — zero tool `ReadOnly = false` em escopo. A regra de ouro (ADE-008 Inv 1) passa a valer **por construção**: não há mais superfície de ação no McpServer.
    *(Fonte: ADE-008 Inv 1, Inv 2, Inv 4 + §Validation item 5; verificado `FifaActionTools.cs`, `AlertWebhookNotifier.cs`, `Program.cs` linhas 47-48, `FifaQueryRepository.cs` cabeçalho "Acesso SOMENTE leitura".)*

12. **AC-12 — Testes atualizados nos 3 projetos:**
    - `Fifa2026.V2.Functions.Tests`: `N8nWebhookNotifierTests.cs` removido inteiro; `PurchaseConsumerFunctionTests.cs` com os 5 testes `Webhook_n8n_*` (linhas 141-244) substituídos por testes equivalentes da notificação inline (dispara em `Inserted`; silenciosa em `Duplicate`/`CategoryNotFound`; nunca lança/propaga).
    - `Fifa2026.V2.McpServer.Tests`: `FifaActionToolsTests.cs` removido inteiro; `McpToolCallIntegrationTests.cs`/`McpEndpointTests.cs` com a asserção de contagem de tools confirmando **7** (não 8).
    - `Fifa2026.V2.FlowEvents.Tests`: `TraceEventMapperTests.cs` e `FlowEventTests.cs` sem casos de teste do nó n8n; `Theory` dos hops ajustada para 5 casos.
    *(Fonte: ADE-008 §Impact "testes associados"; verificado File Lists de 2.4/2.8/2.6.)*

### Bloco E — Doc-fix M-1 (sinalização, não implementação) + regressão

13. **AC-13 — Doc-fix M-1 sinalizado (não aplicado nesta story):** `docs/architecture/ade-000-microservice-parallel-pattern.md` (linha 70: *"Service Bus hops: `ApplicationProperties["CorrelationId"]`..."*) e `docs/stories/2.6.story.md` (AC-4 hop 2, linhas 45-52; Dev Notes linhas 212, 234-235) afirmam que o `correlationId` viaja em `ApplicationProperties`; a implementação real usa **corpo da mensagem + `ILogger.BeginScope`**. Esta story **aponta** a correção (Task 4) — a edição textual de `ade-000-microservice-parallel-pattern.md` é **autoridade do `@architect`** (ADE) e a de `2.6.story.md` seria reabrir uma story **Done** (violaria `story-lifecycle.md`); `@sm` não edita nenhum dos dois. O DoD do épico ("Doc-fix M-1 aplicado") é responsabilidade do **`@po`**, per ADE-008 Change Log.
    *(Fonte: ADE-008 §"Correção de doc herdada (M-1 do gate S2.6)"; verificado `ade-000-microservice-parallel-pattern.md` linhas 70-73, `2.6.story.md` linhas 45-52/391.)*

14. **AC-14 — ADE-004, ADE-007 e ADE-000 (exceto doc-fix M-1) permanecem inalteradas:** esta story **não toca** `PurchaseEntryFunction.cs`, `src/Fifa2026.V2.Gateway/`, nem qualquer linha do pipeline `oid → X-Entra-OID → entra_oid`. O McpServer permanece com ingress interno, atrás do gateway YARP, sem revalidar JWT (herdado, inalterado).
    *(Fonte: ADE-008 Inv 6 + §Validation item 8.)*

15. **AC-15 — Grep de regressão = 0 ocorrências:** `N8N_` / `n8n` / `criar_alerta_ingresso` não aparecem mais em `src/Fifa2026.V2.Functions/`, `src/Fifa2026.V2.McpServer/`, nem em `Lovable/World Cup Tickets Hub/src/lib/flowNodes.ts` / `FlowDiagram.tsx` (fora de comentários históricos explícitos que documentem a remoção, se o `@dev` optar por deixar uma nota).
    *(Fonte: EPIC-003 DoD — "Grep de regressão: `N8N_`/`n8n`/`criar_alerta_ingresso` = 0 ocorrências".)*

16. **AC-16 — Anti-hallucination (Art. IV):** a notificação inline reusa exatamente o padrão já existente (`ILogger.LogInformation` + `BeginScope` da linha 62) — nenhuma classe, campo ou biblioteca nova é inventada. Nenhum nome de tool/endpoint MCP inventado (as 7 tools remanescentes são exatamente as da Story 2.8).

---

## `[AUTO-DECISION]` — as 2 pendências de design da ADE-008, resolvidas aqui

A ADE-008 deixou explicitamente 2 decisões "a confirmar no draft da story" (§Verificação na fonte da verdade + Decisões pendentes do épico). Resolvo ambas agora, com recomendação e justificativa — não deixo vago:

### (a) Forma concreta da notificação inline: HTTP mock vs só log → **LOG ESTRUTURADO**

**Decisão:** a notificação inline é **um `ILogger.LogInformation`**, sem nenhuma chamada `HttpClient`.

**Por quê (justificativa):**
- **Ambas as formas honram a decisão do owner** ("mock HTTP/log") — a ADE-008 é explícita que as duas são válidas. Entre as duas, log é estritamente mais simples.
- **Elimina a ÚLTIMA dependência de rede/configuração do pós-compra.** Um HTTP mock (ex.: `httpbin.org/post`, usado no F4 antigo para o AI Agent) ainda exigiria uma URL configurável (novo App Setting), timeout, tratamento de falha de rede — exatamente a cerimônia operacional que a ADE-008 existe para eliminar. Ninguém mais inspeciona uma UI do n8n para "ver o webhook chegar"; o valor pedagógico de uma chamada HTTP de fato desapareceu junto com o n8n.
- **É trivialmente observável.** Um log dentro do `BeginScope` já correlacionado aparece no App Insights exatamente como qualquer outro hop — e é exatamente isso que o F6 consome (`AppInsightsFlowEventRepository` consulta `traces`, não faz HTTP). Um log estruturado é **mais alinhado** ao mecanismo real do F6 do que uma chamada HTTP seria.
- **Reduz superfície de teste.** Testar "um log foi emitido com este correlationId" é mais simples e mais rápido (sem `HttpMessageHandler` stub) do que testar uma chamada HTTP mockada — reduzindo o atrito de manter os testes herdados de `N8nWebhookNotifierTests`.
- **Alternativa registrada (não escolhida):** se o `@architect`/owner quiser reintroduzir um efeito HTTP visível no futuro (ex.: para uma demo mais "palpável"), isso pode ser adicionado depois como um `IHttpClientFactory` opcional atrás de um App Setting vazio-por-padrão (mesmo padrão fail-open do `N8nWebhookNotifier` antigo) — mas **não é o default desta story**.

### (b) F6 = 5 nós vs manter um 6º nó `NOTIFICATION_SENT` → **5 NÓS (default da ADE-008)**

**Decisão:** `FlowEventType` fica com **5 membros** (0-4). A notificação pós-compra fica **implícita**, "dobrada" dentro do nó `FUNCTION_CONSUMER_DONE` (índice 3) — não ganha nó próprio.

**Por quê (justificativa):**
- **É o default explícito da ADE-008** ("o default desta decisão é 5 nós") — a opção de um 6º nó foi **deferida ao `@sm`/`@dev`**, não recomendada.
- **Consistência com a razão de ser da simplificação.** Reintroduzir um nó dedicado para uma notificação que agora é só um log dentro do mesmo hop do Consumer recriaria visualmente a impressão de uma "etapa extra" que a remoção do n8n justamente eliminou — indo contra a lição didática pretendida ("por que o pós-compra ficou mais simples").
- **Trade-off aceito é honesto e já documentado na ADE-008** ("a notificação pós-compra fica invisível na animação... pequena perda didática, aceita em nome da simplificação"). A Story 3.4 (F6 lab) inclui uma AC específica para tornar essa perda **didaticamente explícita** (o aluno inspeciona o payload do nó Consumer e vê a notificação ali dentro), compensando a perda de um nó visual com uma explicação guiada.
- **Alternativa registrada (não escolhida):** um 6º nó `NOTIFICATION_SENT` alimentado pelo trace da notificação inline — tecnicamente trivial de adicionar depois (é só reintroduzir um membro de enum + um branch de classificação), portanto **não é uma porta fechada**, apenas não é o default.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Architecture (remoção de componente, re-escopo de fronteira — mesma natureza da ADE-008 que a story realiza)
**Secondary Type(s):** API (mudança de comportamento da `PurchaseConsumerFunction`), Integration (McpServer tools/list muda de 8 para 7)
**Complexity:** HIGH (toca 3 projetos .NET + 1 projeto frontend; é uma remoção de componente cross-cutting; embora cada edição individual seja mecânica — em maioria deleções — a superfície tocada é ampla e o risco de regressão silenciosa, exatamente por ser "só remoção", é real)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — as 3 frentes de código (Functions, McpServer, FlowEvents+front) + atualização de testes
- @architect (Aria) — quality gate (autora da ADE-008; valida completude da remoção via §Validation)

**Supporting Agents:**
- @qa (Quinn) — cobertura de regressão (grep + build + test nos 3 projetos)
- @devops (Gage) — confirma o slot da branch `phase-07-final-rearch`, coordena o push

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco em nenhuma referência residual a `IN8nWebhookNotifier`/`IAlertWebhookNotifier`, `dotnet build` 0 erros nos 3 projetos
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — foco em regressão (nenhum arquivo fora do escopo n8n/F6 tocado)
- [ ] Pre-Deployment: N/A — esta story não deploya (deploy real acontece via 3.2/3.3/3.4)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (bloqueia se persistir após 2 iterações)
- HIGH issues: auto_fix (documenta como debt se falhar)

### CodeRabbit Focus Areas

**Primary Focus:**
- Regra de ouro: confirmar que **nenhuma** tool remanescente no McpServer tem `ReadOnly = false` (grep + `tools/list` esperado = 7)
- Idempotência: a notificação inline dispara **só** em `Inserted` — nunca em `Duplicate`/`CategoryNotFound` (regressão dos testes herdados de F4)
- Grep de regressão: `N8N_`/`n8n`/`criar_alerta_ingresso` = 0 ocorrências em `src/`

**Secondary Focus:**
- Renumeração do enum `FlowEventType`: `SQL_INSERTED` deve ser `4`, não sobrar gap nem duplicar ordinal (o `NodeIndex` do `FlowEvent.cs` deriva do ordinal — um erro aqui quebra a animação silenciosamente)
- `TraceEventMapper.Classify` sem regressão nos branches remanescentes (a ordem de avaliação importa — remover o branch n8n não pode fazer um trace do Consumer ser classificado errado)
- Zero mudança fora de escopo em `PurchaseEntryFunction.cs`/`Gateway/` (ADE-004/007 preservadas)

---

## Tasks / Subtasks

### Task 1 — Branch (AC: 1)

- [ ] 1.1 Criar branch **`phase-07-final-rearch`** a partir de **`phase-04-quartas`** (branch atual, cumulativa — confirmado via `git merge-base --is-ancestor phase-04-quartas main` que `phase-04-quartas` NÃO está em `main`, portanto é a base correta). @devops confirma o slot final no push.

### Task 2 — Pós-compra inline (AC: 2, 3, 4, 5)

- [ ] 2.1 Remover o campo `_n8nNotifier` e o parâmetro `IN8nWebhookNotifier n8nNotifier` do construtor de `PurchaseConsumerFunction` (`src/Fifa2026.V2.Functions/Functions/PurchaseConsumerFunction.cs`, linhas 20-34).
- [ ] 2.2 Substituir o bloco `try { await _n8nNotifier.NotifyPurchaseAsync(...) } catch (Exception ex) { _logger.LogWarning(...) }` (linhas 83-102) por um único `_logger.LogInformation(...)` dentro do mesmo `case InsertOutcome.Inserted:`, ainda dentro do `using (_logger.BeginScope(...))` aberto na linha 62. Sketch não-normativo:
  ```csharp
  // ADE-008 Inv 3 — notificação pós-compra INLINE (sem n8n): log estruturado com o
  // trace correlacionado (mesmo BeginScope da linha 62, ADE-000 Inv 5). Um log nunca
  // lança — a defesa em profundidade do try/catch anterior deixa de ser necessária.
  _logger.LogInformation(
      "Notificação pós-compra enviada (inline, sem orquestração externa): " +
      "matchId={MatchId} category={Category} (correlationId={CorrelationId}).",
      message.MatchId, message.Category, message.CorrelationId);
  ```
- [ ] 2.3 Remover a interface `IN8nWebhookNotifier` do `using` no topo do arquivo se não houver mais referência.

### Task 3 — Remover código legado n8n de `Fifa2026.V2.Functions` (AC: 10)

- [ ] 3.1 Remover `src/Fifa2026.V2.Functions/Data/N8nWebhookNotifier.cs`, `IN8nWebhookNotifier.cs`, `N8nWebhookPayload.cs`.
- [ ] 3.2 Remover de `src/Fifa2026.V2.Functions/Program.cs` (linhas 22-29) o comentário "Story 2.4 (F4)" e o registro `services.AddHttpClient<IN8nWebhookNotifier, N8nWebhookNotifier>(...)`.
- [ ] 3.3 Remover de `src/Fifa2026.V2.Functions/local.settings.json` (linhas 18-19) as chaves `//N8N_WEBHOOK_URL` e `N8N_WEBHOOK_URL`.

### Task 4 — F6: 6 → 5 nós (AC: 6, 7, 8, 9)

- [ ] 4.1 Em `src/Fifa2026.V2.FlowEvents/Models/FlowEventType.cs`: remover `N8N_WEBHOOK_TRIGGERED = 4`; renumerar `SQL_INSERTED` de `5` para `4`. Atualizar o comentário de cabeçalho ("Os 6 tipos de evento" → "Os 5 tipos de evento"; "índice ordinal (0..5)" → "(0..4)").
- [ ] 4.2 Em `src/Fifa2026.V2.FlowEvents/Data/TraceEventMapper.cs`: remover o branch `if (msg.Contains("webhook n8n") || msg.Contains("n8n") || role.Contains("n8n")) return FlowEventType.N8N_WEBHOOK_TRIGGERED;` (linhas 56-60). Atualizar comentário de cabeçalho ("6 hops conhecidos" → "5 hops conhecidos").
- [ ] 4.3 Atualizar `src/Fifa2026.V2.FlowEvents.Tests/TraceEventMapperTests.cs` e `FlowEventTests.cs` — remover casos de teste do nó n8n; a `Theory` dos hops passa a cobrir 5 casos.
- [ ] 4.4 Em `Lovable/World Cup Tickets Hub/src/lib/flowNodes.ts`: remover a entrada `index: 4` (n8n); mudar a entrada `SQL` de `index: 5` para `index: 4`. Atualizar o comentário de cabeçalho (lista dos nós).
- [ ] 4.5 Em `Lovable/World Cup Tickets Hub/src/components/flow/FlowDiagram.tsx`: atualizar comentário de cabeçalho (linha 3-7, remover menção ao nó n8n), `aria-label` (linha 55, "6 nós" → "5 nós"), e o `grid-cols` se o layout de 5 colunas exigir ajuste do breakpoint `lg:grid-cols-6`.
- [ ] 4.6 Confirmar (leitura, sem alteração) que `AppInsightsFlowEventRepository.cs`, `FlowHub.cs`, `SignalRFlowEventPublisher.cs`, `FlowEndpoints.cs` **não precisam mudar** — são agnósticos à lista de nós (ADE-008 Inv 5).

### Task 5 — Remover a "mão" e o código legado n8n de `Fifa2026.V2.McpServer` (AC: 11, 12)

- [ ] 5.1 Remover `src/Fifa2026.V2.McpServer/Tools/FifaActionTools.cs` (tool `criar_alerta_ingresso`).
- [ ] 5.2 Remover `src/Fifa2026.V2.McpServer/Data/AlertWebhookNotifier.cs`, `IAlertWebhookNotifier.cs`, `AlertPayloads.cs`.
- [ ] 5.3 Remover de `src/Fifa2026.V2.McpServer/Program.cs` (linhas 47-48) `services.AddHttpClient(AlertWebhookNotifier.HttpClientName, ...)` e `services.AddScoped<IAlertWebhookNotifier, AlertWebhookNotifier>()`.
- [ ] 5.4 Remover a chave `N8N_ALERT_WEBHOOK_URL` de qualquer `local.settings.json`/`appsettings.json` do McpServer (path a confirmar por `@dev` — padrão esperado espelha o de Functions).
- [ ] 5.5 Remover `src/Fifa2026.V2.McpServer.Tests/FifaActionToolsTests.cs` inteiro.
- [ ] 5.6 Atualizar `src/Fifa2026.V2.McpServer.Tests/McpToolCallIntegrationTests.cs` e `McpEndpointTests.cs`: a asserção de contagem de `tools/list` volta de 8 para **7**; remover qualquer caso `tools/call` de `criar_alerta_ingresso`.

### Task 6 — Doc-fix M-1 (sinalização) (AC: 13)

- [ ] 6.1 Registrar formalmente, no gate desta story (`@architect`) e no handoff a `@po`, que `docs/architecture/ade-000-microservice-parallel-pattern.md` (linha 70) e `docs/stories/2.6.story.md` (AC-4 hop 2) precisam de correção textual: onde dizem `ApplicationProperties["CorrelationId"]`, a fonte real é **corpo da mensagem + `ILogger.BeginScope`**. `@sm` não edita nenhum dos dois arquivos (fora de escopo de autoridade — ADE é `@architect`; story Done é `@po`/lifecycle).
- [ ] 6.2 Não criar nenhum código ou teste que dependa de `ApplicationProperties["CorrelationId"]` — o código real (linha 62 de `PurchaseConsumerFunction.cs`) já está correto; é só a documentação que está desatualizada.

### Task 7 — Regressão e validação (AC: 14, 15, 16)

- [ ] 7.1 `dotnet build` + `dotnet test` em `Fifa2026.V2.Functions.Tests`, `Fifa2026.V2.McpServer.Tests`, `Fifa2026.V2.FlowEvents.Tests` — 0 erros, 0 regressão fora do escopo n8n/F6.
- [ ] 7.2 Grep de regressão: `N8N_` / `n8n` / `criar_alerta_ingresso` = 0 ocorrências em `src/Fifa2026.V2.Functions/`, `src/Fifa2026.V2.McpServer/`, `Lovable/World Cup Tickets Hub/src/lib/flowNodes.ts`, `FlowDiagram.tsx`.
- [ ] 7.3 Confirmar via `git diff --stat` que **nenhuma** linha de `PurchaseEntryFunction.cs`, `src/Fifa2026.V2.Gateway/**` foi tocada.
- [ ] 7.4 Revisão anti-hallucination: nenhuma classe/campo/biblioteca nova fora do padrão já existente no repo.

---

## Dev Notes

### Arquitetura técnica delta (o que MUDA vs Quartas/EPIC-002)

```
[PurchaseConsumerFunction]
  ANTES: case Inserted → try { await _n8nNotifier.NotifyPurchaseAsync(...) } catch { LogWarning }
  DEPOIS: case Inserted → _logger.LogInformation("Notificação pós-compra enviada (inline)...")
  (idêntico: só dispara em Inserted; BeginScope/correlationId inalterados)

[FlowEventType]  6 membros (0-5) → 5 membros (0-4); N8N_WEBHOOK_TRIGGERED removido; SQL_INSERTED 5→4
[TraceEventMapper.Classify]  branch n8n (linhas 56-60) removido
[flowNodes.ts / FlowDiagram.tsx]  6 nós → 5 nós (front espelha o backend)

[FifaActionTools.criar_alerta_ingresso]  REMOVIDO — McpServer volta a ter só as 7 tools da Fase A
[AlertWebhookNotifier / N8nWebhookNotifier]  REMOVIDOS — zero HttpClient para n8n em qualquer projeto

[PurchaseEntryFunction]  NÃO MUDA (issuer-agnóstico, fora do escopo desta story)
[Gateway YARP]  NÃO MUDA (ADE-004 preservada)
[FifaQueryRepository]  NÃO MUDA (já só SELECT — a regra de ouro fica trivial "de graça")
```

### Arquivos tocados (File List esperada)

| Arquivo | Ação |
|---|---|
| `src/Fifa2026.V2.Functions/Functions/PurchaseConsumerFunction.cs` | Modificado (remove `IN8nWebhookNotifier`, notificação inline) |
| `src/Fifa2026.V2.Functions/Data/N8nWebhookNotifier.cs` | Removido |
| `src/Fifa2026.V2.Functions/Data/IN8nWebhookNotifier.cs` | Removido |
| `src/Fifa2026.V2.Functions/Data/N8nWebhookPayload.cs` | Removido |
| `src/Fifa2026.V2.Functions/Program.cs` | Modificado (remove registro DI) |
| `src/Fifa2026.V2.Functions/local.settings.json` | Modificado (remove `N8N_WEBHOOK_URL`) |
| `src/Fifa2026.V2.Functions.Tests/N8nWebhookNotifierTests.cs` | Removido |
| `src/Fifa2026.V2.Functions.Tests/PurchaseConsumerFunctionTests.cs` | Modificado (testes `Webhook_n8n_*` → notificação inline) |
| `src/Fifa2026.V2.FlowEvents/Models/FlowEventType.cs` | Modificado (6→5 membros) |
| `src/Fifa2026.V2.FlowEvents/Data/TraceEventMapper.cs` | Modificado (remove branch n8n) |
| `src/Fifa2026.V2.FlowEvents.Tests/TraceEventMapperTests.cs` | Modificado |
| `src/Fifa2026.V2.FlowEvents.Tests/FlowEventTests.cs` | Modificado |
| `src/Fifa2026.V2.McpServer/Tools/FifaActionTools.cs` | Removido |
| `src/Fifa2026.V2.McpServer/Data/AlertWebhookNotifier.cs` | Removido |
| `src/Fifa2026.V2.McpServer/Data/IAlertWebhookNotifier.cs` | Removido |
| `src/Fifa2026.V2.McpServer/Data/AlertPayloads.cs` | Removido |
| `src/Fifa2026.V2.McpServer/Program.cs` | Modificado (remove registro DI) |
| `src/Fifa2026.V2.McpServer.Tests/FifaActionToolsTests.cs` | Removido |
| `src/Fifa2026.V2.McpServer.Tests/McpToolCallIntegrationTests.cs` | Modificado (contagem 8→7) |
| `src/Fifa2026.V2.McpServer.Tests/McpEndpointTests.cs` | Modificado (contagem 8→7) |
| `Lovable/World Cup Tickets Hub/src/lib/flowNodes.ts` | Modificado (6→5 nós) |
| `Lovable/World Cup Tickets Hub/src/components/flow/FlowDiagram.tsx` | Modificado (comentário/aria-label/grid) |

**NÃO tocam (fora de escopo, ADEs preservadas):** `PurchaseEntryFunction.cs`, `src/Fifa2026.V2.Gateway/**`, `FifaQueryRepository.cs`, `FifaTickerTools.cs` (nota: o arquivo se chama `FifaTickerTools.cs` mas declara a classe `FifaTicketTools` — inconsistência pré-existente, não é desta story corrigir), `AppInsightsFlowEventRepository.cs`, `FlowHub.cs`, `FlowEndpoints.cs`, `infra/phase-06/**`, todas as migrations SQL.

### Verificação na fonte da verdade (o que já li e confirmei — Art. IV)

| Afirmação | Confirmado em |
|---|---|
| Notificação n8n só em `Inserted`; `BeginScope` na linha 62; payload sai do corpo | `PurchaseConsumerFunction.cs` linhas 20-120 |
| `N8nWebhookNotifier` = fire-and-forget, no-op se `N8N_WEBHOOK_URL` vazio | `N8nWebhookNotifier.cs` |
| `criar_alerta_ingresso` = `ReadOnly` omitido (default `false`), dispara `AlertWebhookNotifier` | `FifaActionTools.cs` |
| `FifaQueryRepository` só `SELECT`, cabeçalho "nunca grava" | `FifaQueryRepository.cs` linhas 1-16 |
| `FlowEventType` 6 membros (0-5); `N8N_WEBHOOK_TRIGGERED = 4` | `FlowEventType.cs` |
| `NodeIndex => (int)EventType` — a animação depende do ordinal | `FlowEvent.cs` linha 25 |
| `TraceEventMapper.Classify` branch n8n nas linhas 56-60 | `TraceEventMapper.cs` |
| `flowNodes.ts` 6 nós, `FlowDiagram.tsx` grid `lg:grid-cols-6`, `aria-label` "6 nós" | `flowNodes.ts`, `FlowDiagram.tsx` |
| Doc M-1: `ade-000...md` linha 70 e `2.6.story.md` linhas 45-52 dizem `ApplicationProperties`; código real é corpo+BeginScope | grep confirmado |
| `phase-04-quartas` NÃO é ancestral de `main` (base correta para a branch nova) | `git merge-base --is-ancestor` exit 1 |

### Testing

**Test file locations:**
- `src/Fifa2026.V2.Functions.Tests/PurchaseConsumerFunctionTests.cs` (modificado)
- `src/Fifa2026.V2.McpServer.Tests/McpToolCallIntegrationTests.cs`, `McpEndpointTests.cs` (modificados)
- `src/Fifa2026.V2.FlowEvents.Tests/TraceEventMapperTests.cs`, `FlowEventTests.cs` (modificados)

**Test standards:**
- xUnit + Moq (herdado — `Mock<IPurchaseRepository>`, sem mais `Mock<IN8nWebhookNotifier>`)
- Regressão de idempotência: teste equivalente a `Webhook_n8n_is_fired_on_Inserted`/`_NOT_fired_on_Duplicate`/`_NOT_fired_on_CategoryNotFound`, adaptado para verificar o `ILogger` (`Mock<ILogger<T>>.Verify(...)` ou captura via `ITestOutputHelper`/log provider de teste) em vez de verificar chamada ao notifier.
- `tools/list` count: `McpToolCallIntegrationTests`/`McpEndpointTests` devem afirmar **7**, não 8.
- `TraceEventMapperTests`: `Theory` com 5 casos (não 6); nenhum caso testando o texto "n8n"/"webhook n8n".

### Troubleshooting esperado

| Sintoma | Causa | Mitigação |
|---|---|---|
| Animação da bolinha "pula" um nó ou trava no nó 3 | `NodeIndex` do front não bate com o ordinal do backend pós-renumeração | Confirmar `flowNodes.ts` `index: 4` para `SQL` E `FlowEventType.SQL_INSERTED = 4` no backend — devem estar sincronizados (fonte única em ambos os lados, não há geração automática) |
| `tools/list` ainda retorna 8 | `FifaActionTools.cs` removido mas `Program.cs` do McpServer ainda referencia `WithToolsFromAssembly` de forma que capturou um assembly cacheado, ou o arquivo não foi de fato deletado | Confirmar remoção física do arquivo + rebuild limpo (`obj`/`bin` — Gotcha conhecido do McpServer, ver Story 2.9 Dev Agent Record "Gotcha 3") |
| Teste de idempotência falha após a troca do notifier por log | Mock de `ILogger` não capturando `LogInformation` corretamente (nível de log/categoria) | Usar `Mock<ILogger<PurchaseConsumerFunction>>` com `Verify(x => x.Log(LogLevel.Information, ...))` ou helper de captura já usado em outros testes do projeto |

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-30 | 0.1 | Story drafted — linchpin do EPIC-003. 16 ACs derivados da ADE-008 Inv 1-6 + ADE-000 Inv 4/5. Resolvidas as 2 decisões de design pendentes da ADE-008: (a) notificação inline = log estruturado (não HTTP mock) — mais simples, honra a decisão do owner, elimina última dependência de rede; (b) F6 = 5 nós (default ADE-008), 6º nó `NOTIFICATION_SENT` registrado como alternativa considerada e não escolhida. Doc-fix M-1 sinalizado como Task (não implementado — fora da autoridade de edição do `@sm`/`@dev`: ADE é `@architect`, story Done é `@po`). Verificado linha-a-linha contra `PurchaseConsumerFunction.cs`, `FlowEventType.cs`, `TraceEventMapper.cs`, `FifaActionTools.cs`, `AlertWebhookNotifier.cs`, `flowNodes.ts`, `FlowDiagram.tsx`, `Program.cs` (Functions + McpServer). Confirmado via `git merge-base` que a branch nova deve partir de `phase-04-quartas` (não está em `main`). | @sm (River) |
| 2026-06-30 | 0.1.1 | Validated GO (10/10) — Status: Draft → Ready. 16 ACs rastreiam à ADE-008 Inv 1-6 + ADE-000 Inv 4/5; verificado contra código real (PurchaseConsumerFunction.cs l.62 e 83-102, FlowEventType.cs 6 membros, FifaActionTools.cs ReadOnly=false, flowNodes.ts 6 nós). @po CONCORDA com as 2 AUTO-DECISIONs: (a) log estruturado — mais alinhado ao motor do F6 (que lê traces, não HTTP); (b) F6=5 nós é o default explícito da ADE-008. Linchpin confirmado: bloqueia 3.2/3.3/3.4. | @po (Pax) |

---

## Dev Agent Record

### Agent Model Used

_Pendente — a preencher pelo `@dev` na implementação._

### Completion Notes

_Pendente._

### File List

_Pendente — ver tabela "Arquivos tocados" em Dev Notes como baseline esperado._

---

## QA Results

_Pendente — gate `@architect` (Aria), tools: code-review, pattern-validation, architecture-review, regression-validation. Aplicar o §Validation (8 itens) da ADE-008 como checklist — a maioria cai sobre esta story (itens 1, 2, 3 parcial, 4, 5, 7 parcial, 8); itens 3 (infra) e 6 (ADE-002 MOOT) são de 3.2/@architect respectivamente._
